from django.urls import path

# API endpoints - będą dodawane w kolejnych krokach MVP
urlpatterns = [
    # TODO: auth endpoints (login, logout, me, set-password, reset)
    # TODO: timesheet endpoints (month, day, save)
    # TODO: tasks endpoints (list, filters)
]
